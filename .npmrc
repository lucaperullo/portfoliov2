# Registry (puoi usare il registry di default o uno privato come Nexus o Verdaccio)
registry=https://registry.npmjs.org/

# Numero massimo di tentativi di connessione
fetch-retries=5

# Timeout per ogni richiesta di connessione
fetch-timeout=60000

# Directory per memorizzare i pacchetti cache
cache=/home/username/.npm-cache

# Directory per i log delle operazioni npm
loglevel=info

# Configurazione di default per evitare problemi con i permessi (soprattutto su Linux)
unsafe-perm=true
